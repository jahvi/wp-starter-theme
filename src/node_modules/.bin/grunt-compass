#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../grunt-compass/bin/grunt-compass" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../grunt-compass/bin/grunt-compass" "$@"
  ret=$?
fi
exit $ret
