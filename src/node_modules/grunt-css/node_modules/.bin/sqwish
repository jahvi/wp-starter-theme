#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../sqwish/bin/sqwish" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../sqwish/bin/sqwish" "$@"
  ret=$?
fi
exit $ret
